<?xml version="1.0" encoding="UTF-8"?>

<configuration debug="true" scan="true" scanPeriod="60 seconds">
    <springProfile name="local">
        <property resource="logback-local.properties"/>
    </springProfile>

    <springProperty scope="context" name="LOG_LEVEL" source="logging.level.root"/>
    <property resource="application.properties" />

    <springProperty name="dd" source="spring.datasource.driver-class-name"/>
    <springProperty name="kyle" source="spring.datasource.url"/>
    <springProperty name="bum" source="spring.datasource.username"/>
    <springProperty name="sika" source="spring.datasource.password"/>

    <property name="LOG_PATH" value="${log.config.path}"/>
    <property name="LOG_FILE_NAME" value="${log.config.filename}"/>
    <property name="ERR_LOG_FILE_NAME" value="err_log"/>
    <property name="LOG_PATTERN" value="%-5level %d{yy-MM-dd HH:mm:ss}[%thread] [%logger{0}:%line] - %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE_NAME}.%d{yyyy-MM-dd}_%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>1</maxHistory>
        </rollingPolicy>
    </appender>

    <appender name="DB" class="ch.qos.logback.classic.db.DBAppender">
        <connectionSource class="ch.qos.logback.core.db.DriverManagerConnectionSource">
            <driverClass>${dd}</driverClass>
            <url>${kyle}</url>
            <user>${bum}</user>
            <password>${sika}</password>
        </connectionSource>
    </appender>

    <root level="${LOG_LEVEL}">
<!--        <appender-ref ref="CONSOLE"/>-->
        <appender-ref ref="DB"/>
<!--        <appender-ref ref="FILE"/>-->
    </root>

<!--    <logger name="com.example.loggingexample.sample" level="DEBUG" additivity="false">-->
<!--        <appender-ref ref="CONSOLE"/>-->
<!--        <appender-ref ref="FILE"/>-->
<!--        <appender-ref ref="Error"/>-->
<!--        <appender-ref ref="DB"/>-->
<!--    </logger>-->

</configuration>


<!--        SELECT * FROM LOGGING_EVENT_PROPERTY;-->
<!--        select * from LOGGING_EVENT ;-->
<!--        select * from LOGGING_EVENT_EXCEPTION ;-->
